import React from "react";
import ReactDOM from "react-dom/client";
import HarmonyHealth from "./HarmonyHealth.jsx";
import "./index.css";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <HarmonyHealth />
  </React.StrictMode>,
);
